const Login = ({ Card }) => {
  return (
    <div className="Login">
      <Card
        Header="Sign In"
        Text="Please use this form if you already have an account here."
      ></Card>
    </div>
  );
};

export default Login;
